name: Submit Google Sitemap

on:
  workflow_dispatch:
  schedule:
    - cron: '0 2 * * 0'  # Every Sunday at 2:00 AM UTC

jobs:
  sitemap_submission:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout workflows repo
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        python -m pip install requests google-api-python-client google-auth google-auth-oauthlib google-auth-httplib2
        python -c "import google; print('google path:', google.__path__)"
        python -c "from google.oauth2 import service_account; print('google.oauth2 OK')"

    - name: Submit sitemaps to Google
      env:
        GOOGLE_CREDENTIALS_JSON: ${{ secrets.GOOGLE_CREDENTIALS_JSON }}
      run: |
        python scripts/google/sitemaps.py
