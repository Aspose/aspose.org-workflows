name: products.aspose.org-svg
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.SVG
      family_slug: svg
      content_dir: content/products.aspose.org/en/svg
      output_mode: family
    secrets: inherit