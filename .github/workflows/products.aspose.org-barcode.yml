name: products.aspose.org-barcode
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.BarCode
      family_slug: barcode
      content_dir: content/products.aspose.org/en/barcode
      output_mode: family
    secrets: inherit