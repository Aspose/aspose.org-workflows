name: products.aspose.org-slides
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Slides
      family_slug: slides
      content_dir: content/products.aspose.org/en/slides
      output_mode: family
    secrets: inherit