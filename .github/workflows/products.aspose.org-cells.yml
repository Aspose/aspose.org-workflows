name: products.aspose.org-cells
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Cells
      family_slug: cells
      content_dir: content/products.aspose.org/cells/en
      theme: products
      staging_host: products-qa.aspose.org
      production_host: products.aspose.org
      staging_deploy_url: s3://products-qa.aspose.org/?prefix=cells/&region=us-west-2
      production_deploy_url: s3://products.aspose.org/?prefix=cells/&region=us-west-2
      staging_distribution_id: E8DUFGQ3MLGGR
      production_distribution_id: E1WTAPBGIKFKJJ
      output_mode: family
      ceph_production_deploy_url: s3://products-aspose-org/?prefix=cells/&endpoint=https://s3.dynabic.com&region=us-east-1
    secrets: inherit
