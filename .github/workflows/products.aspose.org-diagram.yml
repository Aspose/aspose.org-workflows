name: products.aspose.org-diagram
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Diagram
      family_slug: diagram
      content_dir: content/products.aspose.org/en/diagram
      output_mode: family
    secrets: inherit