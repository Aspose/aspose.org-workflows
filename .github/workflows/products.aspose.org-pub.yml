name: products.aspose.org-pub
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.PUB
      family_slug: pub
      content_dir: content/products.aspose.org/en/pub
      output_mode: family
    secrets: inherit