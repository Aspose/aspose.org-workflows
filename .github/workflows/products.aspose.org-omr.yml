name: products.aspose.org-omr
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.OMR
      family_slug: omr
      content_dir: content/products.aspose.org/en/omr
      output_mode: family
    secrets: inherit