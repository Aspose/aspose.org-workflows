name: products.aspose.org-3d
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.3D
      family_slug: 3d
      content_dir: content/products.aspose.org/en/3d
      output_mode: family
    secrets: inherit