name: products.aspose.org-drawing
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Drawing
      family_slug: drawing
      content_dir: content/products.aspose.org/en/drawing
      output_mode: family
    secrets: inherit