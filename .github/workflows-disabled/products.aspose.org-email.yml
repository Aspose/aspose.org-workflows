name: products.aspose.org-email
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Email
      family_slug: email
      content_dir: content/products.aspose.org/en/email
      output_mode: family
    secrets: inherit