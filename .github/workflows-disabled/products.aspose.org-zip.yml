name: products.aspose.org-zip
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.ZIP
      family_slug: zip
      content_dir: content/products.aspose.org/en/zip
      output_mode: family
    secrets: inherit