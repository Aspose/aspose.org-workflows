These workflows are intentionally disabled because the current tracked
`aspose.org` content tree does not contain the corresponding content roots.

Move a workflow file back into `.github/workflows/` if its content returns
and the deployment path is needed again.
