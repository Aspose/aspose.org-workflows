name: products.aspose.org-medical
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Medical
      family_slug: medical
      content_dir: content/products.aspose.org/en/medical
      output_mode: family
    secrets: inherit