name: products.aspose.org-tasks
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Tasks
      family_slug: tasks
      content_dir: content/products.aspose.org/en/tasks
      output_mode: family
    secrets: inherit