name: products.aspose.org-words
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Words
      family_slug: words
      content_dir: content/products.aspose.org/en/words
      output_mode: family
    secrets: inherit