name: products.aspose.org-html
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.HTML
      family_slug: html
      content_dir: content/products.aspose.org/en/html
      output_mode: family
    secrets: inherit