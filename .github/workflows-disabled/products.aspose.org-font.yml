name: products.aspose.org-font
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.Font
      family_slug: font
      content_dir: content/products.aspose.org/en/font
      output_mode: family
    secrets: inherit