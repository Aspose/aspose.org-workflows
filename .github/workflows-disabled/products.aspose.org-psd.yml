name: products.aspose.org-psd
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.PSD
      family_slug: psd
      content_dir: content/products.aspose.org/en/psd
      output_mode: family
    secrets: inherit