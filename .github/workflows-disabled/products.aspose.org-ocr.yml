name: products.aspose.org-ocr
on:
  workflow_dispatch:
    inputs:
      environment:
        description: Build environment
        type: environment
        default: staging
        required: true
jobs:
  deploy:
    uses: ./.github/workflows/products.family.reusable.yml
    with:
      environment: ${{ github.event.inputs.environment }}
      display_name: Aspose.OCR
      family_slug: ocr
      content_dir: content/products.aspose.org/en/ocr
      output_mode: family
    secrets: inherit